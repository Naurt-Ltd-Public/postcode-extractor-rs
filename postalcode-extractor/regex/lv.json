{
	"country": "LV",
	"comment": "Latvian postcode is in the form `LV-1234`, and must have the `LV-` to be valid",
	"priority": 100,
	"regex": {
		"engines": {
			"rust": "\\b(?<postcode>LV-\\d{4})\\b"
		},
		"position_logic": {
			"operation": "GT",
			"position": 0.5
		}
	}
}